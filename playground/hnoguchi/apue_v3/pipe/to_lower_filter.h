#ifndef TO_LOWER_FILTER_H
# define TO_LOWER_FILTER_H

#include "err_func.h"
#include <ctype.h>

// err_func.c
void	err_sys(const char *fmt, ...);
void	err_ret(const char *fmt, ...);
void	err_quit(const char *fmt, ...);
void	err_dump(const char *fmt, ...);

#endif
